// import { Router } from "express";
// import { addNewProduct, getProductById, updateProduct, deleteProduct, allProducts } from "../controller/ProductController";

// const router = Router();

// router.route("/:storeId").post(addNewProduct);
// router.route("/:storeId/:id").get(getProductById).put(updateProduct).delete(deleteProduct);
// router.route("/:storeId").get(allProducts);

// export { router as ProductRouter };