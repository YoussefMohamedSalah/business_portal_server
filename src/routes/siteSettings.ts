// import { Router } from "express";
// import { getSiteSettings, addSiteSettings, updateSiteSettings, deleteSiteSettings } from "../controller/SiteSettingsController";

// const router = Router();

// router.route("/:storeId").post(addSiteSettings);
// router.route("/:storeId").get(getSiteSettings).put(updateSiteSettings).delete(deleteSiteSettings);

// export { router as SiteSettingsRouter };